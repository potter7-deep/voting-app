# Server Configuration
PORT=5000
NODE_ENV=production

# Database (SQLite - for simple deployments)
DB_PATH=./voting_system.sqlite

# Or PostgreSQL (recommended for production)
# Uncomment below and comment DB_PATH above for PostgreSQL
# DB_DIALECT=postgres
# PGHOST=your-db-host.render.com
# PGPORT=5432
# PGDATABASE=voting_db
# PGUSER=voting
# PGPASSWORD=your-password

# JWT - Generate a secure secret for production
# Use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Rate Limiting (optional - defaults are provided)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
LOGIN_RATE_LIMIT_WINDOW_MS=900000
LOGIN_RATE_LIMIT_MAX_ATTEMPTS=5

# CORS - Specify your production domains (comma-separated)
# For Render: https://your-app-name.onrender.com
CORS_ORIGIN=https://your-app-name.onrender.com

